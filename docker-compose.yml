services:
  monitor-bot:
    build: .
    container_name: discord_monitor_bot
    restart: always
    # .env ファイルを読み込む設定
    env_file:
      - .env
    # ホストマシンの正確なリソース（特にディスクやプロセス）を監視するため、
    # ルートディレクトリをコンテナ内にマウントします
    volumes:
      - /:/host:ro
